import 'package:flutter/material.dart';

class HospitalPanel extends StatelessWidget {
  const HospitalPanel({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(height: 500, color: Colors.yellow,);
  }
}
