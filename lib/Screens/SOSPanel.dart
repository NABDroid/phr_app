import 'package:flutter/material.dart';

class SOSPanel extends StatelessWidget {
  const SOSPanel({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(height: 500, color: Colors.green,);
  }
}
